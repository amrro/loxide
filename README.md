# Loxide
Rust 🦀 + Crafting Interpreters


## Try

```cmd
> ./target/debug/loxide -h

Loxide: A simple interpreter with a REPL.

Usage: loxide <COMMAND>

Commands:
  tokenize   Tokenize a source file and output the resulting tokens.
  interpret  Start an interactive REPL to evaluate expressions.
  help       Print this message or the help of the given subcommand(s)

Options:
  -h, --help     Print help (see more with '--help')
  -V, --version  Print version
```
